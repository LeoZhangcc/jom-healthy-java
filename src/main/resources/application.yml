server:
  port: 12200
spring:
  application:
    name: jom-healthy
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://mysql-jomhealthy-jomhealthy.b.aivencloud.com:12100/jomhealthy?useSSL=true&verifyServerCertificate=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  thymeleaf:
    cache: false
  profiles:
    active: dev
